#if (${PACKAGE_NAME} && ${PACKAGE_NAME} != "")package ${PACKAGE_NAME};
/*
*
*

This class ${NAME} is created and managed by ${USER}
Created on ${DATE} at ${TIME} for the project ${PROJECT}

*
*
*/

#end
#parse("File Header.java")
public class ${NAME} {
}
